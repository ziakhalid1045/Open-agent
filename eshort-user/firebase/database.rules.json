{
  "rules": {
    "typing": {
      "$chatRoomId": {
        "$userId": {
          ".read": true,
          ".write": "auth != null && auth.uid === $userId"
        }
      }
    },
    "status": {
      "$userId": {
        ".read": true,
        ".write": "auth != null && auth.uid === $userId"
      }
    }
  }
}
